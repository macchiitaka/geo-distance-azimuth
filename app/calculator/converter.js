/**
 * deg to rad
 *
 * @param {number} num deg
 * @return {number} deg
 */
exports.toRad = num => num * (Math.PI / 180);

/**
 * deg to rad
 *
 * @param {number} num rad
 * @return {number} deg
 */
exports.toDeg = num => num * (180 / Math.PI);
